android {
            defaultConfig {
                applicationId = "org.nativescript.checkHealth"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }